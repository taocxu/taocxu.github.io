name: Apply July 19 batch 2 refinements

on:
  push:
    branches:
      - feature/google-sites-v2
    paths:
      - scripts/apply_july19_batch2.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out development branch
        uses: actions/checkout@v4
        with:
          ref: feature/google-sites-v2
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply requested refinements
        run: python scripts/apply_july19_batch2.py

      - name: Commit refinements
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html LINK_INVENTORY.md scripts/audit_links.py
          git commit -m "Apply requested academic website refinements"
          git push origin HEAD:feature/google-sites-v2
